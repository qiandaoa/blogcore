import axios from "axios"

var root = "后端接口地址"


axios.get("url",{请求体}).then().catch()
// ({}) : 表示空对象,method:方法名(get，post,put,delete)，params(地址后面的参数),
function toType(obj) {
    return ({}).toString
}